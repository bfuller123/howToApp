//var
var topics = [];


//API 



$("search-button").on("click", function() {
    var searchInput = $("search-nav").input.val().trim();
    if (loggedIn) {
        if (topics.contains())
    } else {

    }
})
